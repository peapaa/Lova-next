// mui
import { Button } from "@mui/material";

const ButtonSearch = () => {
  return (
    <Button type="submit" variant="contained">
      Search
    </Button>
  );
};

export default ButtonSearch;
